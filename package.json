{
  "name": "v4-prizes-subgraph",
  "license": "GPL-3.0",
  "version": "1.0.0",
  "scripts": {
    "reinstall": "rm -rf node_modules/ && yarn",
    "clean": "rm -rf build/ generated/",
    "format": "prettier --config .prettierrc --write \"**/*.{ts,js}\"",
    "format:file": "prettier --config .prettierrc --write",
    "prepare:optimism": "mustache networks/optimism.json subgraph.template.yaml subgraph.optimism.yaml",
    "prepare:polygon": "mustache networks/polygon.json subgraph.template.yaml subgraph.polygon.yaml",
    "prepare:mainnet": "mustache networks/mainnet.json subgraph.template.yaml subgraph.mainnet.yaml",
    "prepare:avalanche": "mustache networks/avalanche.json subgraph.template.yaml subgraph.avalanche.yaml",
    "gen:optimism": "graph codegen subgraph.optimism.yaml",
    "gen:polygon": "graph codegen subgraph.polygon.yaml",
    "gen:mainnet": "graph codegen subgraph.mainnet.yaml",
    "gen:avalanche": "graph codegen subgraph.avalanche.yaml",
    "deploy:optimism": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/optimism-v4-prizes-claimed subgraph.optimism.yaml",
    "deploy:polygon": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/polygon-v4-prizes-claimed subgraph.polygon.yaml",
    "deploy:mainnet": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/mainnet-v4-prizes-claimed subgraph.mainnet.yaml",
    "deploy:avalanche": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pooltogether/avalanche-v4-prizes-claimed subgraph.avalanche.yaml",
    "all-optimism": "yarn clean && yarn prepare:optimism && yarn gen:optimism && yarn deploy:optimism",
    "all-polygon": "yarn clean && yarn prepare:polygon && yarn gen:polygon && yarn deploy:polygon",
    "all-mainnet": "yarn clean && yarn prepare:mainnet && yarn gen:mainnet && yarn deploy:mainnet",
    "all-avalanche": "yarn clean && yarn prepare:avalanche && yarn gen:avalanche && yarn deploy:avalanche",
    "build:mainnet": "graph build subgraph.mainnet.yaml",
    "auth": "graph auth https://api.thegraph.com/deploy/",
    "tag-release": "PACKAGE_VERSION=$(./scripts/package-version.sh) && git tag -ae v$PACKAGE_VERSION && git push --tags",
    "test": "yarn clean && yarn prepare:rinkeby && yarn gen:rinkeby && cp subgraph.rinkeby.yaml subgraph.yaml && graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0",
    "@pooltogether/v4-core": "^1.2.0",
    "matchstick-as": "0.4.2",
    "mustache": "4.2.0"
  },
  "devDependencies": {
    "prettier": "2.6.0"
  }
}
